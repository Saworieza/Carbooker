<%= form_with(model: store_car, local: true) do |form| %>
  <% if store_car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(store_car.errors.count, "error") %> prohibited this store_car from being saved:</h2>

      <ul>
      <% store_car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :store_id %>
    <%= form.collection_select(:store_id, Store.all, :id, :name) %>
  </div>

  <div class="field">
    <%= form.label :car_id %>
    <%= form.collection_select(:car_id, Car.all, :id, :name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
